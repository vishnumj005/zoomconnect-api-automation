pipeline {
  agent any
  stages {
    stage('version') {
      steps {
        sh 'python3 --version'
      }
    }
    stage('Run Tests') {
      steps {
        sh "behave --tags=@api_current_balance"
      }
    }
  }
}