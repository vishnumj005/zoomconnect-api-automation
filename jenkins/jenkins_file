pipeline {
  agent any
  stages {
    stage('version') {
      steps {
        bat 'python3 --version'
      }
    }
    stage('Run Tests') {
      steps {
        bat "behave --tags=@api_current_balance"
      }
    }
  }
}